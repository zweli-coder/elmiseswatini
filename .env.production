# Docker Hub username — update with your actual username
DOCKER_USERNAME=your_docker_username

# Database credentials — change these to strong values
DB_USER=postgres
DB_PASSWORD=change_this_to_strong_password
DB_NAME=eswatini_lmis

# JWT secret — generate a strong random key
JWT_SECRET=generate_a_strong_random_jwt_secret_here

# Node environment
NODE_ENV=production
